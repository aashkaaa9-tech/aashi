# aashi
A simple Python CLI tool to manage shared expenses. Add users, record bills, auto-split amounts, and calculate who owes whom. Stores data in JSON and can export settlements as CSV. Works like a minimal Splitwise for terminal users.
